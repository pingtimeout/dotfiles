- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.config/yamllint: yamllint
    ~/.config/kitty: kitty
    ~/.gitconfig: gitconfig
    ~/.gitignore: gitignore
    ~/.grc: grc-confs
    ~/.zpreztorc: zpreztorc
    ~/.zshrc: zshrc

- shell:
  - [git submodule update --init --recursive, Installing submodules]
